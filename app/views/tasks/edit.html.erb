<h1>id: <%= @task.id %> のtask編集ページ</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'task' %>
  <%= f.text_field :content %>

  <%= f.submit '登録' %>
<% end %>

<%= link_to 'task list に戻る', task_path %>